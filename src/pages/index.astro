---
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroGrid from '../components/HeroGrid.astro';
import { plans, siteConfig } from '../data/site';

const pageTitle = 'North Star Digital | AI-Powered Growth Systems — Wisconsin';
const description =
  'North Star Digital builds, manages, and runs complete AI-powered websites, SEO, and automated lead follow-up systems for Wisconsin contractors and service businesses — 100% done for you. Plans starting at $299/mo.';

const phone = '(262) 254-0128';
const phoneHref = 'tel:+12622540128';
---

<BaseLayout title={pageTitle} description={description} path="/">

  <!-- ═══ HERO ═══ -->
  <section class="hero-glow relative overflow-hidden px-4 py-20 md:py-32">
    <canvas id="star-canvas" class="pointer-events-none absolute inset-0 h-full w-full opacity-0" style="transition: opacity 1.2s ease;"></canvas>
    <div id="hero-grid-container" class="pointer-events-none absolute inset-0 opacity-0" style="transition: opacity 2.5s ease;">
      <HeroGrid />
    </div>
    <div class="pointer-events-none absolute left-1/4 top-1/4 h-96 w-96 rounded-full bg-[#2E8BFF] opacity-10 blur-3xl"></div>
    <div class="pointer-events-none absolute right-1/4 top-1/2 h-64 w-64 rounded-full bg-[#06D6FF] opacity-10 blur-3xl"></div>

    <div class="relative mx-auto max-w-5xl text-center">
      <p class="animate-fade-in-up badge-teal mb-6 inline-block rounded-full border px-4 py-1 text-xs font-semibold uppercase tracking-widest">
        Wisconsin — AI-Powered Growth Agency
      </p>
      <h1 class="animate-fade-in-up animate-delay-100 text-4xl font-bold leading-tight text-white md:text-6xl">
        Done-For-You AI Systems<br class="hidden md:block" />
        <span class="gradient-text">That Get Your Phone Ringing.</span>
      </h1>
      <p class="animate-fade-in-up animate-delay-200 mx-auto mt-6 max-w-2xl text-lg leading-relaxed text-[#8EA8C3]">
        We build, manage, and run complete AI-powered websites, local SEO, and automated lead follow-up systems for Wisconsin contractors and service businesses — <span class="font-semibold text-white">100% done for you, so you can focus on the work.</span>
      </p>
      <div class="animate-fade-in-up animate-delay-300 mt-8 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
        <a
          href="/contact"
          class="btn-glow inline-flex items-center gap-2 rounded-xl bg-[#2E8BFF] px-8 py-4 text-base font-bold text-white shadow-lg transition-all duration-200 hover:scale-105 hover:bg-[#1E7AEB]"
        >
          Apply for a Strategy Session
        </a>
        <a
          href="/contact"
          class="inline-flex items-center gap-2 rounded-xl border-2 border-[#06D6FF]/40 px-8 py-4 text-base font-semibold text-white transition-all duration-200 hover:border-[#06D6FF] hover:bg-[#06D6FF]/10"
        >
          Ask an Expert
        </a>
      </div>
      <p class="animate-fade-in-up animate-delay-400 mt-4 text-sm text-[#8EA8C3]">Free consultation · No contracts · Results in 30 days</p>

      <!-- Stats row -->
      <div class="animate-fade-in-up animate-delay-500 mx-auto mt-14 grid max-w-2xl grid-cols-3 gap-4 border-t border-white/10 pt-10">
        <div>
          <p class="counter-num text-2xl font-bold text-white" data-target="30" data-suffix=" days">30 days</p>
          <p class="mt-1 text-xs text-[#8EA8C3]">Results guarantee</p>
        </div>
        <div>
          <p class="text-2xl font-bold text-[#06D6FF]">$299<span class="text-base font-normal text-[#8EA8C3]">/mo</span></p>
          <p class="mt-1 text-xs text-[#8EA8C3]">Plans starting from</p>
        </div>
        <div>
          <p class="text-2xl font-bold text-white">100%</p>
          <p class="mt-1 text-xs text-[#8EA8C3]">Done For You</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ SOCIAL PROOF BAR ═══ -->
  <div class="divider-glow"></div>
  <section class="border-b border-[#E3E8F1] bg-[#F0F6FF] px-4 py-5">
    <div class="mx-auto max-w-5xl">
      <div class="flex flex-wrap items-center justify-center gap-6 text-sm font-semibold text-[#44506A]">
        <span class="flex items-center gap-2"><span class="text-[#06D6FF]">✓</span> 30-Day Money-Back Guarantee</span>
        <span class="hidden text-[#DDE5F2] md:block">|</span>
        <span>✓ AI-Powered Systems</span>
        <span class="hidden text-[#DDE5F2] md:block">|</span>
        <span>✓ 100% Done For You</span>
        <span class="hidden text-[#DDE5F2] md:block">|</span>
        <span>✓ Father-Son Team — Wisconsin</span>
      </div>
    </div>
  </section>
  <div class="divider-glow"></div>

  <!-- ═══ SERVICES ═══ -->
  <section class="section-light px-4 py-20 md:py-24">
    <div class="mx-auto max-w-5xl">
      <div class="reveal mb-12 text-center">
        <p class="mb-2 text-xs font-semibold uppercase tracking-widest text-[#06D6FF]">What We Do</p>
        <h2 class="text-3xl font-bold text-[#0B2545] md:text-4xl">AI-Powered Growth Systems,<br class="hidden md:block" /> Built and Managed For You</h2>
        <p class="mx-auto mt-4 max-w-2xl text-base text-[#44506A]">We don't just build it and leave. Every service is fully set up, managed, and optimized by our team — automatically, without you lifting a finger.</p>
      </div>
      <div class="grid gap-6 md:grid-cols-2">
        <div class="card-hover reveal rounded-2xl border border-[#E3E8F1] bg-white p-8 shadow-sm">
          <h3 class="text-xl font-bold text-[#0B2545]">High-Converting Website</h3>
          <p class="mt-2 text-sm leading-relaxed text-[#44506A]">We design, build, host, and maintain your website. Fast, mobile-ready, and built around one goal: turning visitors into phone calls. No tech skills needed on your end — ever.</p>
        </div>
        <div class="card-hover reveal rounded-2xl border border-[#E3E8F1] bg-white p-8 shadow-sm" style="transition-delay:0.1s">
          <h3 class="text-xl font-bold text-[#0B2545]">Local SEO</h3>
          <p class="mt-2 text-sm leading-relaxed text-[#44506A]">We handle all the ongoing SEO work — Google Business Profile, local citations, keyword rankings, and monthly reports. You show up first. We do all the work.</p>
        </div>
        <div class="card-hover reveal rounded-2xl border border-[#E3E8F1] bg-white p-8 shadow-sm" style="transition-delay:0.2s">
          <h3 class="text-xl font-bold text-[#0B2545]">CRM & Lead Management</h3>
          <p class="mt-2 text-sm leading-relaxed text-[#44506A]">We set up and run a complete lead tracking system. Every inquiry is captured, followed up with automatically, and tracked until it closes. You never lose a lead again.</p>
        </div>
        <div class="card-hover reveal rounded-2xl border border-[#E3E8F1] bg-white p-8 shadow-sm" style="transition-delay:0.3s">
          <h3 class="text-xl font-bold text-[#0B2545]">Marketing Automation</h3>
          <p class="mt-2 text-sm leading-relaxed text-[#44506A]">Automated follow-up sequences, review requests, appointment reminders — all built, configured, and managed by us. Your pipeline runs itself around the clock.</p>
        </div>
      </div>

      <!-- AI Voice Agent — Interactive Live Demo -->
      <div class="reveal mt-6">
        <div class="card-hover-ai ai-scan relative rounded-2xl border-2 border-[#2E8BFF]/40 bg-gradient-to-br from-[#0B2545] to-[#0d2f5e] p-8 shadow-lg overflow-hidden">
          <div class="grid gap-8 md:grid-cols-2 md:items-center">

            <!-- Left: copy + play control -->
            <div>
              <div class="mb-3 flex items-center gap-3">
                <span class="inline-flex items-center gap-1.5 rounded-full border border-[#06D6FF]/40 bg-[#06D6FF]/10 px-3 py-1 text-xs font-bold uppercase tracking-widest text-[#06D6FF]"><span class="live-dot"></span> LIVE — AI Voice Agent</span>
              </div>
              <h3 class="text-xl font-bold text-white">Hear It Answer a Real Call</h3>
              <p class="mt-2 text-sm leading-relaxed text-[#8EA8C3]">An AI that answers your phone 24/7, qualifies the lead, and books the job — even when you're out on a site. Press play and listen to exactly how a live call sounds.</p>
              <button id="voice-demo-play" type="button" class="btn-glow-teal mt-5 inline-flex items-center gap-2 rounded-xl border-2 border-[#06D6FF]/50 px-6 py-3 text-sm font-bold text-[#06D6FF] transition-all hover:border-[#06D6FF] hover:bg-[#06D6FF]/10">
                <svg id="voice-demo-icon" class="h-4 w-4" fill="currentColor" viewBox="0 0 24 24"><path d="M8 5v14l11-7z" /></svg>
                <span id="voice-demo-label">Play Live Demo</span>
              </button>
              <p id="voice-demo-hint" class="mt-2 text-xs text-[#6E86A6]">Plays a short scripted call out loud using your device's voice. Tap to listen.</p>
            </div>

            <!-- Right: live phone transcript -->
            <div class="rounded-2xl border border-[#06D6FF]/20 bg-[#071a33]/70 p-5 backdrop-blur">
              <div class="mb-4 flex items-center justify-between border-b border-white/10 pb-3">
                <div class="flex items-center gap-2 text-xs font-semibold text-[#8EA8C3]">
                  <span id="voice-demo-dot" class="h-2 w-2 rounded-full bg-[#6E86A6]"></span>
                  <span id="voice-demo-status">Incoming call…</span>
                </div>
                <span class="text-xs font-medium text-[#6E86A6]">North Star AI Agent</span>
              </div>
              <div id="voice-demo-transcript" class="flex min-h-[252px] flex-col gap-3 overflow-y-auto text-sm" style="max-height:300px;">
                <p id="voice-demo-placeholder" class="m-auto px-4 text-center text-sm text-[#6E86A6]">Press <span class="font-semibold text-[#06D6FF]">Play Live Demo</span> to watch the AI handle an inbound call in real time.</p>
              </div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ HOW IT WORKS ═══ -->
  <section class="hero-glow relative overflow-hidden px-4 py-20 md:py-24">
    <HeroGrid />
    <div class="relative mx-auto max-w-5xl">
      <div class="reveal mb-12 text-center">
        <p class="mb-2 text-xs font-semibold uppercase tracking-widest text-[#06D6FF]">Simple Process</p>
        <h2 class="text-3xl font-bold text-white md:text-4xl">Up and Running in 3 Steps</h2>
        <p class="mx-auto mt-4 max-w-xl text-base text-[#8EA8C3]">No confusing onboarding. No back-and-forth for months. We handle everything and move fast.</p>
      </div>
      <div class="grid gap-8 md:grid-cols-3">
        <div class="reveal text-center">
          <div class="btn-glow-teal mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full border border-[#06D6FF]/40 bg-[#06D6FF]/10 text-xl font-bold text-[#06D6FF]">1</div>
          <h3 class="text-lg font-bold text-white">Strategy Session</h3>
          <p class="mt-2 text-sm leading-relaxed text-[#8EA8C3]">We review your business, your goals, and exactly what's holding you back online. Honest, no-pitch conversation.</p>
        </div>
        <div class="reveal text-center" style="transition-delay:0.15s">
          <div class="btn-glow-teal mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full border border-[#06D6FF]/40 bg-[#06D6FF]/10 text-xl font-bold text-[#06D6FF]">2</div>
          <h3 class="text-lg font-bold text-white">We Build &amp; Run Everything</h3>
          <p class="mt-2 text-sm leading-relaxed text-[#8EA8C3]">Our team builds your site, configures your AI systems, sets up your CRM, and manages everything ongoing. You don't touch a thing.</p>
        </div>
        <div class="reveal text-center" style="transition-delay:0.3s">
          <div class="btn-glow-teal mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full border border-[#06D6FF]/40 bg-[#06D6FF]/10 text-xl font-bold text-[#06D6FF]">3</div>
          <h3 class="text-lg font-bold text-white">Your Phone Rings</h3>
          <p class="mt-2 text-sm leading-relaxed text-[#8EA8C3]">Leads come in, follow-ups go out automatically, and your pipeline fills up. You focus on doing the work — we handle the rest.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ WHO WE ARE ═══ -->
  <section class="section-light px-4 py-20 md:py-24">
    <div class="mx-auto max-w-5xl">
      <div class="grid gap-12 md:grid-cols-2 md:items-center">
        <div class="reveal-left">
          <p class="mb-2 text-xs font-semibold uppercase tracking-widest text-[#06D6FF]">Who We Are</p>
          <h2 class="text-3xl font-bold text-[#0B2545] md:text-4xl">A Local Team That Actually Cares</h2>
          <p class="mt-4 text-base leading-relaxed text-[#44506A]">
            North Star Digital is a father-son AI agency based in Wisconsin. We're not a faceless corporation or an overseas call center — we're a dedicated local team that treats every client like a partner.
          </p>
          <p class="mt-4 text-base leading-relaxed text-[#44506A]">
            We started NSD because we saw too many great local businesses losing jobs online — not because their work was bad, but because their digital systems were. We fix that with modern AI tools, and we manage everything so you never have to think about it.
          </p>
          <p class="mt-4 text-base font-semibold text-[#0B2545]">We fix that. Fast.</p>
          <div class="mt-8 flex flex-wrap gap-4">
            <a href="/about" class="inline-flex rounded-xl border border-[#06D6FF]/40 px-6 py-3 text-sm font-semibold text-[#2E8BFF] transition-all duration-200 hover:border-[#06D6FF] hover:bg-[#06D6FF]/5">Read Our Story →</a>
          </div>
        </div>
        <div class="reveal space-y-4">
          <div class="card-hover rounded-2xl border border-[#E3E8F1] bg-[#F4F6FA] p-6">
            <p class="text-sm font-bold text-[#0B2545]">Headquartered in Wisconsin</p>
            <p class="mt-1 text-sm text-[#44506A]">We're a real team with a real address. Not a call center, not an overseas agency — people you can actually meet.</p>
          </div>
          <div class="card-hover rounded-2xl border border-[#E3E8F1] bg-[#F4F6FA] p-6">
            <p class="text-sm font-bold text-[#0B2545]">100% Done For You</p>
            <p class="mt-1 text-sm text-[#44506A]">We build it, we run it, we manage it. You don't need any tech experience — just tell us your goals and we handle everything else.</p>
          </div>
          <div class="card-hover rounded-2xl border border-[#E3E8F1] bg-[#F4F6FA] p-6">
            <p class="text-sm font-bold text-[#0B2545]">Fair — Transparent Pricing</p>
            <p class="mt-1 text-sm text-[#44506A]">One monthly price covers everything. No surprise bills. No confusing add-ons. No contracts.</p>
          </div>
          <div class="card-hover rounded-2xl border border-[#E3E8F1] bg-[#F4F6FA] p-6">
            <p class="text-sm font-bold text-[#0B2545]">Honest — We'll Tell You the Truth</p>
            <p class="mt-1 text-sm text-[#44506A]">If something isn't working, we'll tell you. We're here for your long-term growth, not just your first invoice.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ FOUNDING CLIENTS (honest social proof — no fabricated reviews; swap in real verified reviews once clients close) ═══ -->
  <section class="bg-[#F0F6FF] px-4 py-20 md:py-24">
    <div class="divider-glow-subtle mb-0"></div>
    <div class="mx-auto max-w-4xl">
      <div class="reveal mb-10 text-center">
        <p class="mb-2 text-xs font-semibold uppercase tracking-widest text-[#06D6FF]">Founding Clients</p>
        <h2 class="text-3xl font-bold text-[#0B2545] md:text-4xl">Be One of Our First Wisconsin Wins</h2>
        <p class="mx-auto mt-4 max-w-2xl text-base text-[#44506A]">We're a new father-son agency taking on a small number of founding clients — which means senior attention, founder-level pricing, and a team that treats your results like our reputation depends on it. Because right now, it does.</p>
      </div>
      <div class="reveal grid gap-6 md:grid-cols-3">
        <div class="card-hover rounded-2xl border border-[#E3E8F1] bg-white p-6 text-center shadow-sm">
          <p class="text-xl font-bold text-[#2E8BFF]">Founder-Led</p>
          <p class="mt-2 text-sm text-[#44506A]">You work directly with Dillon and Guy — not a junior account rep.</p>
        </div>
        <div class="card-hover rounded-2xl border border-[#E3E8F1] bg-white p-6 text-center shadow-sm" style="transition-delay:0.1s">
          <p class="text-xl font-bold text-[#2E8BFF]">30-Day Guarantee</p>
          <p class="mt-2 text-sm text-[#44506A]">Measurable progress in 30 days or we keep working free.</p>
        </div>
        <div class="card-hover rounded-2xl border border-[#E3E8F1] bg-white p-6 text-center shadow-sm" style="transition-delay:0.2s">
          <p class="text-xl font-bold text-[#2E8BFF]">No Contracts</p>
          <p class="mt-2 text-sm text-[#44506A]">Stay because it works — not because you're locked in.</p>
        </div>
      </div>
      <div class="reveal mt-8 text-center">
        <a href="/services#pricing" class="btn-glow inline-flex items-center gap-2 rounded-xl bg-[#2E8BFF] px-8 py-4 text-base font-bold text-white shadow-lg transition-all duration-200 hover:scale-105 hover:bg-[#1E7AEB]">Explore Bundles Made For You</a>
      </div>
    </div>
  </section>

  <!-- ═══ PRICING TEASER ═══ -->
  <section class="section-light px-4 py-20 md:py-24">
    <div class="mx-auto max-w-5xl">
      <div class="reveal mb-12 text-center">
        <p class="mb-2 text-xs font-semibold uppercase tracking-widest text-[#06D6FF]">Simple Pricing</p>
        <h2 class="text-3xl font-bold text-[#0B2545] md:text-4xl">One Plan. Everything Included.</h2>
        <p class="mx-auto mt-4 max-w-xl text-base text-[#44506A]">Pick a plan and get a complete, fully-managed AI growth system — website, SEO, CRM, automation, and ongoing support. No hidden fees, no contracts.</p>
      </div>
      <div class="reveal grid gap-6 md:grid-cols-3">
        {plans.map((plan) => (
          <div class={`card-hover relative rounded-2xl bg-white p-8 text-center ${plan.popular ? 'border-2 border-[#2E8BFF] shadow-lg' : 'border border-[#E3E8F1] shadow-sm'}`}>
            {plan.popular && (
              <p class="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full bg-[#2E8BFF] px-4 py-1 text-xs font-bold text-white">Most Popular</p>
            )}
            <p class="text-xs font-semibold uppercase tracking-widest text-[#2E8BFF]">{plan.name}</p>
            <p class="mt-3 text-4xl font-bold text-[#0B2545]">{plan.price}<span class="text-lg font-normal text-[#44506A]">{plan.period}</span></p>
            <p class="mt-3 text-sm text-[#44506A]">{plan.summary}</p>
            <a href="/services#pricing" class={`mt-6 block rounded-xl py-3 text-sm font-bold transition ${plan.popular ? 'bg-[#2E8BFF] text-white hover:bg-[#1E7AEB]' : 'border border-[#2E8BFF] text-[#2E8BFF] hover:bg-[#F0F6FF]'}`}>See What's Included</a>
          </div>
        ))}
      </div>
      <div class="reveal mt-8 text-center">
        <p class="text-sm text-[#44506A]">Not sure which plan? <a href={phoneHref} class="font-semibold text-[#2E8BFF] hover:underline">Call us — {phone}</a> and we'll be honest with you.</p>
      </div>
    </div>
  </section>

  <!-- ═══ GUARANTEE ═══ -->
  <section class="section-light px-4 pb-20 md:pb-24">
    <div class="mx-auto max-w-4xl">
      <div class="reveal guarantee-splash rounded-3xl border-2 border-[#2E8BFF]/20 bg-gradient-to-br from-[#F0F6FF] to-[#EBF8FF] p-10 text-center md:p-16">
        <p class="mb-3 text-xs font-semibold uppercase tracking-widest text-[#2E8BFF]">Our Promise</p>
        <h2 class="text-3xl font-bold text-[#0B2545] md:text-4xl">30-Day Results Guarantee</h2>
        <p class="mx-auto mt-5 max-w-2xl text-lg leading-relaxed text-[#44506A]">
          If you don't see measurable progress in your first 30 days — more traffic, better rankings, or more inquiries — we keep working at no extra charge until you do. No excuses. No runaround.
        </p>
        <div class="mt-8 grid gap-4 sm:grid-cols-3">
          <div class="rounded-2xl bg-white p-5 shadow-sm">
            <p class="text-2xl font-bold text-[#2E8BFF]">30</p>
            <p class="mt-1 text-sm font-semibold text-[#0B2545]">Days to Results</p>
            <p class="mt-1 text-xs text-[#44506A]">Or we keep working free</p>
          </div>
          <div class="rounded-2xl bg-white p-5 shadow-sm">
            <p class="text-2xl font-bold text-[#2E8BFF]">$0</p>
            <p class="mt-1 text-sm font-semibold text-[#0B2545]">Risk to You</p>
            <p class="mt-1 text-xs text-[#44506A]">No contracts, no fine print</p>
          </div>
          <div class="rounded-2xl bg-white p-5 shadow-sm">
            <p class="text-2xl font-bold text-[#2E8BFF]">100%</p>
            <p class="mt-1 text-sm font-semibold text-[#0B2545]">Accountable</p>
            <p class="mt-1 text-xs text-[#44506A]">We stand behind every project</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ GOOGLE BUSINESS ═══ -->
  <section class="bg-[#F0F6FF] px-4 py-16 md:py-20">
    <div class="mx-auto max-w-5xl">
      <div class="grid gap-10 md:grid-cols-2 md:items-center">
        <div class="reveal">
          <p class="mb-2 text-xs font-semibold uppercase tracking-widest text-[#06D6FF]">Find Us on Google</p>
          <h2 class="text-3xl font-bold text-[#0B2545] md:text-4xl">Locally Rooted in Wisconsin</h2>
          <p class="mt-4 text-base leading-relaxed text-[#44506A]">
            We're a real local business — not a remote agency, not a call center. Based in Wisconsin, we work with service businesses across the state and beyond.
          </p>
          <p class="mt-4 text-base leading-relaxed text-[#44506A]">
            Find us on Google Maps, leave us a review, or just call. We answer.
          </p>
          <div class="mt-6 flex justify-center">
            <a href={siteConfig.googleMapsUrl} target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 rounded-xl border border-[#2E8BFF] px-6 py-3 text-sm font-bold text-[#2E8BFF] transition hover:bg-[#F0F6FF]">
              Find Us on Google Maps
            </a>
          </div>
        </div>
        <div class="reveal space-y-3">
          <div class="rounded-2xl border border-[#E3E8F1] bg-white p-5 shadow-sm">
            <p class="text-sm font-bold text-[#0B2545]">Based In</p>
            <p class="mt-1 text-sm text-[#44506A]">Wisconsin — serving businesses statewide and nationwide.</p>
          </div>
          <div class="rounded-2xl border border-[#E3E8F1] bg-white p-5 shadow-sm">
            <p class="text-sm font-bold text-[#0B2545]">Industries We Serve</p>
            <p class="mt-1 text-sm text-[#44506A]">HVAC · Plumbing · Electrical · Roofing · Masonry · Landscaping · Home Services</p>
          </div>
          <div class="rounded-2xl border border-[#E3E8F1] bg-white p-5 shadow-sm">
            <p class="text-sm font-bold text-[#0B2545]">Availability</p>
            <p class="mt-1 text-sm text-[#44506A]">Available 7 days a week. We respond fast — usually same day.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ FINAL CTA ═══ -->
  <section class="hero-glow relative overflow-hidden px-4 py-20 md:py-24">
    <HeroGrid />
    <div class="pointer-events-none absolute bottom-0 left-1/2 h-64 w-64 -translate-x-1/2 rounded-full bg-[#06D6FF] opacity-10 blur-3xl"></div>
    <div class="relative mx-auto max-w-3xl text-center">
      <p class="reveal mb-4 text-xs font-semibold uppercase tracking-widest text-[#06D6FF]">Let's Talk</p>
      <h2 class="reveal text-3xl font-bold text-white md:text-5xl">Ready to Get More Calls?</h2>
      <p class="reveal mx-auto mt-6 max-w-xl text-lg text-[#8EA8C3]">
        Talk to a real person today. We'll review your current online presence and tell you exactly what it would take to get your phone ringing more — no pressure, no pitch, 100% honest.
      </p>
      <div class="reveal mt-8 flex justify-center">
        <a href="/contact" class="btn-glow inline-flex items-center gap-2 rounded-xl bg-[#2E8BFF] px-10 py-4 text-lg font-bold text-white shadow-lg transition-all duration-200 hover:scale-105 hover:bg-[#1E7AEB]">
          See How We Can Help
        </a>
      </div>
      <p class="reveal mt-6 text-sm text-[#8EA8C3]">Real people. Real results. No contracts required.</p>
    </div>
  </section>

</BaseLayout>

<script>
  // ── Star field ──
  (function() {
    const canvas = document.getElementById('star-canvas') as HTMLCanvasElement;
    const grid   = document.getElementById('hero-grid-container') as HTMLElement;
    if (!canvas) return;
    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    function resize() {
      canvas.width  = canvas.offsetWidth;
      canvas.height = canvas.offsetHeight;
    }
    resize();
    window.addEventListener('resize', resize);

    const STAR_COUNT = 190;
    const stars = Array.from({ length: STAR_COUNT }, () => ({
      x:       Math.random(),
      y:       Math.random(),
      r:       Math.random() * 1.1 + 0.35,
      alpha:   Math.random() * 0.45 + 0.45,
      speed:   Math.random() * 0.003 + 0.001,
      twinkle: Math.random() * Math.PI * 2,
      color:   Math.random() > 0.75 ? '#06D6FF' : '#ffffff',
    }));

    function drawStars() {
      if (!ctx) return;
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      stars.forEach(s => {
        s.twinkle += s.speed * 2;
        const opacity = (Math.sin(s.twinkle) * 0.4 + 0.6) * s.alpha;
        const cx = s.x * canvas.width;
        const cy = s.y * canvas.height;
        ctx.beginPath();
        ctx.arc(cx, cy, s.r, 0, Math.PI * 2);
        ctx.fillStyle = s.color;
        ctx.globalAlpha = opacity;
        ctx.fill();
        const grd = ctx.createRadialGradient(cx, cy, 0, cx, cy, s.r * 3.5);
        grd.addColorStop(0, s.color);
        grd.addColorStop(1, 'transparent');
        ctx.beginPath();
        ctx.arc(cx, cy, s.r * 3.5, 0, Math.PI * 2);
        ctx.fillStyle = grd;
        ctx.globalAlpha = opacity * 0.25;
        ctx.fill();
        if (s.r > 1.4) {
          ctx.globalAlpha = opacity * 0.6;
          ctx.strokeStyle = s.color;
          ctx.lineWidth = 0.7;
          const len = s.r * 4;
          ctx.beginPath();
          ctx.moveTo(cx - len, cy); ctx.lineTo(cx + len, cy);
          ctx.moveTo(cx, cy - len); ctx.lineTo(cx, cy + len);
          ctx.stroke();
        }
      });
      ctx.globalAlpha = 1;
      requestAnimationFrame(drawStars);
    }
    drawStars();

    setTimeout(() => { canvas.style.opacity = '1'; }, 300);
    setTimeout(() => {
      canvas.style.transition = 'opacity 3s ease';
      canvas.style.opacity = '0';
      if (grid) {
        setTimeout(() => { grid.style.opacity = '1'; }, 1500);
      }
    }, 30000);
  })();

  // ── Scroll reveal — fixed threshold + fallback ──
  const revealEls = document.querySelectorAll('.reveal, .reveal-left');

  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        revealObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.05, rootMargin: '0px 0px -40px 0px' });

  revealEls.forEach(el => revealObserver.observe(el));

  // Safety fallback: show any in-viewport reveals after load
  window.addEventListener('load', () => {
    setTimeout(() => {
      document.querySelectorAll('.reveal, .reveal-left').forEach(el => {
        if (!el.classList.contains('visible')) {
          const rect = el.getBoundingClientRect();
          if (rect.top < window.innerHeight + 100) {
            el.classList.add('visible');
          }
        }
      });
    }, 200);
  });

  // ── Count-up — fixed threshold + fallback ──
  function countUp(el: HTMLElement) {
    const target = parseInt(el.dataset.target || '0');
    const suffix = el.dataset.suffix || '';
    const duration = 1800;
    const step = target / (duration / 16);
    let current = 0;
    const timer = setInterval(() => {
      current += step;
      if (current >= target) {
        current = target;
        clearInterval(timer);
      }
      el.textContent = Math.floor(current) + suffix;
    }, 16);
  }

  const counters = document.querySelectorAll<HTMLElement>('.counter-num[data-target]');
  const counterObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        countUp(entry.target as HTMLElement);
        counterObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });
  counters.forEach(el => counterObserver.observe(el));

  // Fallback: run counters if already in view on load
  window.addEventListener('load', () => {
    setTimeout(() => {
      counters.forEach(el => {
        const rect = el.getBoundingClientRect();
        if (rect.top < window.innerHeight) {
          countUp(el);
          counterObserver.unobserve(el);
        }
      });
    }, 300);
  });
</script>

<script>
  // ── AI Voice Agent — animated transcript + spoken audio demo ──
  (function () {
    const playBtn = document.getElementById('voice-demo-play');
    const transcript = document.getElementById('voice-demo-transcript');
    const statusEl = document.getElementById('voice-demo-status');
    const dotEl = document.getElementById('voice-demo-dot');
    const labelEl = document.getElementById('voice-demo-label');
    const hintEl = document.getElementById('voice-demo-hint');
    if (!playBtn || !transcript) return;

    const lines = [
      { who: 'ai',     text: 'Thanks for calling North Star Digital — this is the AI assistant. How can I help?' },
      { who: 'caller', text: "Hi, I've got a burst pipe in my basement and there's water everywhere." },
      { who: 'ai',     text: "Let's get that handled fast. I can have a licensed plumber out to you today. What's your name and address?" },
      { who: 'caller', text: 'It\'s Dave — 142 Oak Street, Pewaukee.' },
      { who: 'ai',     text: 'Thanks, Dave. I have a 2:30 or a 4:00 open this afternoon. Which works better for you?' },
      { who: 'caller', text: '2:30 is perfect.' },
      { who: 'ai',     text: "You're booked for 2:30. I've texted you a confirmation and the plumber's name. Help is on the way." },
    ];

    const synth = typeof window !== 'undefined' ? window.speechSynthesis : null;
    let playing = false;

    function pickVoice() {
      if (!synth) return null;
      const v = synth.getVoices();
      return v.find(x => /en[-_]US/i.test(x.lang) && /(female|samantha|zira|jenny|aria|google us english)/i.test(x.name))
          || v.find(x => /^en/i.test(x.lang))
          || v[0] || null;
    }
    if (synth) { try { synth.getVoices(); synth.onvoiceschanged = pickVoice; } catch (e) {} }

    const wait = (ms) => new Promise(r => setTimeout(r, ms));

    function speak(text) {
      return new Promise((resolve) => {
        if (!synth) return resolve();
        try {
          const u = new SpeechSynthesisUtterance(text);
          const voice = pickVoice();
          if (voice) u.voice = voice;
          u.rate = 1.03; u.pitch = 1.0;
          u.onend = resolve; u.onerror = resolve;
          synth.speak(u);
          // safety: never hang if onend never fires
          setTimeout(resolve, Math.min(9000, 600 + text.length * 80));
        } catch (e) { resolve(); }
      });
    }

    function addBubble(who) {
      const wrap = document.createElement('div');
      wrap.className = 'flex flex-col ' + (who === 'ai' ? 'items-start' : 'items-end');
      const label = document.createElement('span');
      label.className = 'mb-1 text-[10px] font-bold uppercase tracking-widest ' + (who === 'ai' ? 'text-[#06D6FF]' : 'text-[#8EA8C3]');
      label.textContent = who === 'ai' ? 'AI Agent' : 'Caller';
      const bubble = document.createElement('div');
      bubble.className = who === 'ai'
        ? 'max-w-[92%] rounded-2xl rounded-tl-sm border border-[#06D6FF]/30 bg-[#06D6FF]/10 px-4 py-2.5 leading-relaxed text-[#dbe9ff]'
        : 'max-w-[92%] rounded-2xl rounded-tr-sm border border-white/10 bg-white/5 px-4 py-2.5 leading-relaxed text-[#cdd9ea]';
      wrap.appendChild(label); wrap.appendChild(bubble);
      transcript.appendChild(wrap);
      return bubble;
    }

    async function typeInto(el, text, perChar) {
      for (let i = 0; i < text.length; i++) {
        el.textContent += text[i];
        transcript.scrollTop = transcript.scrollHeight;
        await wait(perChar);
      }
    }

    async function run() {
      if (playing) return;
      playing = true;
      playBtn.setAttribute('disabled', 'true');
      playBtn.classList.add('opacity-60', 'pointer-events-none');
      if (labelEl) labelEl.textContent = 'Playing…';
      if (hintEl) hintEl.textContent = 'Live call in progress…';
      transcript.innerHTML = '';
      if (statusEl) statusEl.textContent = 'Call connected';
      if (dotEl) dotEl.className = 'h-2 w-2 rounded-full bg-[#22c55e] animate-pulse';

      for (const line of lines) {
        const bubble = addBubble(line.who);
        if (line.who === 'ai') {
          await Promise.all([ typeInto(bubble, line.text, 16), speak(line.text) ]);
          await wait(250);
        } else {
          await typeInto(bubble, line.text, 26);
          await wait(450);
        }
      }

      if (statusEl) statusEl.textContent = 'Appointment booked';
      if (dotEl) dotEl.className = 'h-2 w-2 rounded-full bg-[#22c55e]';
      if (labelEl) labelEl.textContent = 'Replay Demo';
      if (hintEl) hintEl.textContent = "That entire call was handled with no human involved.";
      playBtn.removeAttribute('disabled');
      playBtn.classList.remove('opacity-60', 'pointer-events-none');
      playing = false;
    }

    playBtn.addEventListener('click', () => { if (synth) try { synth.cancel(); } catch (e) {} run(); });
  })();
</script>
